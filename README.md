# Paper Insight

Paper Insight 是一个本地优先的论文推荐 Web 应用，用来持续同步 arXiv 最新论文，并通过配置的大模型接口对候选论文做分析、打分和总结。

默认主题聚焦“大模型/智能体 + 网络自治/网络数字孪生/系统架构”的研究方向，包括网络自治、自智网络、零接触网络、网络数字孪生、意图网络、LLM Agent、多智能体协同、Agent 工程化集成、闭环自治和面向网络的智能体系统。`5G/6G`、无线网络、网络通信、异常检测、流量预测、根因分析等更具体的领域或业务/运维场景仍可在查询构建器里手动勾选，但不再作为默认强制条件。

## 当前能力

- 从 arXiv RSS 自动同步最新论文到本地论文库。
- 从本地论文库按时间范围和查询条件筛选候选论文。
- 用户确认候选论文后，再逐篇调用所选大模型分析。
- 分析失败后支持从当前失败论文继续重试，已成功分析的论文不会重复调用模型。
- 高于推荐阈值的论文进入推荐列表，其他论文进入隐藏列表；低于阈值的论文不会为了凑数被补进推荐。
- 支持设置“候选目标数”：若当前时间范围内的候选不足，会保持关键词不变，依次扩展到最近 14 天、30 天；范围耗尽后按实际数量展示，不会混入放宽关键词的论文。推荐列表目标和后端分析上限都最高可设为 100。
- 支持针对单个推荐列表生成“精选论文阅读清单”：先选择周报候选子集，再基于论文原文或摘要重新复评、筛选和生成 Markdown，便于手动发布到洞察网站。
- 支持论文探索页，把历史生成过的论文集中浏览。
- 支持摘要翻译，翻译必须通过 LLM API。
- 支持强制使用 arXiv API 重新获取候选，并在界面中标注候选来源。

## 核心流程

1. 在左侧确认或编辑论文搜索条件。
2. 点击“生成推荐列表”。
3. 流程弹窗展示候选获取进度。
4. 候选列表出现后，界面会显示目标数量、初始时间范围命中数和实际使用的时间范围，用户确认要分析的论文。
5. 可选择“强制 arXiv API 重新获取”，绕过本地库直接查询 arXiv API。
6. 所选大模型逐篇分析论文，界面展示当前论文、耗时和进度。
7. 如果首轮阈值以上论文数量低于“最低达标数”，应用会从下一批候选开始新一轮查询和分析；一旦达到最低达标数，就停止继续分析追加候选，而不是从低分论文里补足。候选目标最多 100 篇，推荐列表目标最多 100 篇，自动补轮最多 10 轮。
8. 如果某篇论文分析失败，点击“重试分析”会从失败的当前论文继续，不重做已经成功的论文。
9. 分析完成后生成新的推荐列表。
10. 打开某个推荐列表后，可以点击“准备生成周报”，先从该列表中选择周报候选子集，再基于论文原文重新评分和筛选，最后生成发布版 Markdown。
11. 可以在推荐模式、探索模式和单篇分析页之间切换。

## 论文来源

候选论文只来自 arXiv。默认路径是：

```text
arXiv RSS -> 本地 arXiv 库 -> 候选筛选 -> 用户确认 -> 大模型分析
```

强制获取路径是：

```text
arXiv API -> 候选列表 -> 用户确认 -> 大模型分析
```

候选目标数是期望展示给用户确认的候选数量，不是 arXiv 的固定返回承诺。若当前范围不足，应用仅扩大时间范围（7 天到 14 天、30 天），不放宽关键词；扩展到 30 天仍不足时会明确展示实际数量。

界面会标注每篇候选论文的来源：

- `本地 arXiv 库`：来自后端 RSS 同步后的本地论文库。
- `arXiv API`：来自 `export.arxiv.org/api/query` 的直接查询。
- `arXiv API 缓存`：来自直接 API 查询后的短期缓存或异常兜底。

更完整的论文获取、RSS 同步、本地库、429 处理和大模型输入说明见 [论文获取逻辑说明](PAPER_FETCHING_README.md)。

## 后端自动同步

后端进程会自动维护 arXiv RSS 同步，不依赖系统定时器调用 HTTP 接口。

同步判断逻辑：

- 后端读取 `.cache/arxiv-papers.json` 里的 `lastSyncedAt`。
- 按 `lastSyncedAt + ARXIV_DAILY_SYNC_MS` 计算下一次同步时间。
- 如果服务停了几天，重启后发现本地库过期，会自动同步。
- 如果刚同步过，会等到真正到期再同步。
- 同步失败后会按 `ARXIV_AUTO_SYNC_RETRY_MS` 重新尝试。

默认 RSS 分类：

```text
cs.NI, cs.AI, cs.LG, cs.MA, cs.DC, cs.IT, eess.SP, eess.SY
```

## 大模型分析

论文推荐必须使用配置的大模型接口。应用不会使用本地关键词规则兜底。

推荐分析阶段的大模型输入包括论文标题、作者、分类、日期、arXiv 链接和摘要。生成发布版阅读清单时，服务端会优先抓取 arXiv HTML 原文，清洗并截取摘要、方法、系统、实验、结果、局限和结论等关键段落，先给周报候选重新生成四维分数和总分，再按周报入选阈值筛选并生成 Markdown；如果某篇论文没有可用 HTML 原文，会自动降级为摘要和已有分析复评，不会中断整份周报。

大模型负责生成：

- 分维度评分和兴趣适配分类
- 一句话概要
- 问题、背景、方法、技术细节、贡献、实验、网络价值和局限分析
- 阅读路径和推荐理由
- 摘要翻译

服务端负责基于四维分项重新计算推荐总分，不直接采用模型返回的总分。这样可以保证摘要初筛、周报复评和前端展示使用同一套权重与校准逻辑。

如果 LLM 调用失败、结果缺字段、超时或没有返回某篇论文的分析，服务端会返回错误。应用不会自动无限重试，用户可以在界面上手动确认是否重试；重试会保留已成功分析的结果，并从当前失败论文继续。LLM 返回 JSON 时如果在合法 JSON 后附带少量解释文本，服务端会抽取第一段完整 JSON 对象继续处理；如果 JSON 本身不完整或缺少论文结果，仍会报错。

## 评分维度

当前使用 4 个研究质量维度辅助打分：

- 研究问题价值：20%
- 方法新意：30%
- 系统价值：20%
- 证据强度：30%

网络、AI、ICT、电信或 ADN 方向匹配不混入四维质量分，避免把“题材相关”误判成“研究质量高”。服务端会先按研究问题、方法新意、系统价值和证据强度计算研究质量分，再用兴趣适配做最终推荐分校准：网络自治/电信方向小幅上浮，通用 AI/Agent 方法轻微上浮，专用非目标领域轻度降权，方向不明只轻微后移。兴趣适配只做加减分，不做封顶。

服务端总分计算逻辑：

```text
base = 研究问题价值 * 0.20 + 方法新意 * 0.30 + 系统价值 * 0.20 + 证据强度 * 0.30
balancePenalty = max(0, base - min(方法新意, 证据强度)) * 0.12
weakEvidencePenalty = max(0, 70 - 证据强度) * 0.20
researchQuality = clamp(base * 1.20 - 22 - balancePenalty - weakEvidencePenalty, 0, 100)
最终推荐分 = clamp(researchQuality + interestAdjustment, 0, 100)
```

兴趣适配使用 4 个档位：

| 档位 | 校准 |
| --- | --- |
| 网络自治/电信方向 | `+4`。包括 ADN、O-RAN、5G/6G、网络数字孪生、意图驱动、闭环自治、网络运维、路由/QoS/频谱/切片/故障诊断等。 |
| 通用 AI/Agent 方法 | `+2`。包括不绑定专用行业的 LLM/Agent/RAG/工具调用/评测/安全/系统架构等通用方法。 |
| 专用非目标领域 | `-6`。主问题、评价对象或主要应用场景明确落在医学、生命科学、脑科学、基因组、地理、游戏、教育、金融、法律、社科、推荐系统等专用垂直领域，且摘要没有展示可迁移的通用 AI/Agent/系统方法时使用。若只是使用垂直数据做验证，但主要贡献是通用机制、架构、评测或 agent 方法，则归入通用 AI/Agent 方法。 |
| 方向相关性不明 | `-2`。没有明确目标方向，也看不出通用可迁移方法时使用；这只是轻微后移，不等同于非目标。 |

这意味着方法新意和证据强度仍然是主要拉开差距的质量维度；方向只做最终排序校准。只有业务方向匹配但方法机制、系统设计或证据支撑不足的论文，不会因为 ICT、ADN 或电信场景相关就自动进入高分区；专用非目标领域论文会排后，但质量特别强时仍可能进入 70 分左右的扫读区。

总分档位：

| 分数段 | 含义 |
| --- | --- |
| 0-49 | 基本不达标。概念模糊、贡献弱、证据不足，或只是产业/实践/流程性内容。 |
| 50-59 | 弱相关或弱贡献。有一定问题意识或应用场景，但研究设计、方法机制或证据明显不足。 |
| 60-69 | 一般候选。方向或问题有价值，但创新、系统完整性、证据至少有一项明显短板。 |
| 70-79 | 值得扫读。问题清楚，有一定研究贡献或系统价值，但还不是强论文。 |
| 80-89 | 重点关注。问题、方法、系统或证据中至少两项比较强，值得深入读。 |
| 90-100 | 少数高价值论文。问题重要，方法有实质贡献，证据扎实，结论边界清楚，值得跟踪或复现。 |

分维度评分也使用同样档位：研究问题价值看问题是否清晰、重要、可验证；方法新意看是否有可复用的新机制、建模、任务定义或评测协议；系统价值看解决框架的模块、接口、数据流、闭环机制、部署约束和可复用性；证据强度看实验、数据、基线、消融、泛化、鲁棒性和复现线索。若只能基于摘要和元数据判断，模型必须保守评分并说明限制。

推荐分低于 60 的论文必须给出“不推荐原因”，但它不是分数解释。原因不能写“总分低于 60”“某维度多少分”或模板化阅读建议，而要像评审意见一样指出这篇论文具体差在哪里：例如方法只是流程拼装、缺少可复用机制，系统接口/闭环/失败处理没说清，实验缺少数据集/基线/消融/真实场景，或摘要只能看到愿景而看不到可验证假设。

推荐分 70 及以上的论文必须给出显性的“高分信号”。一句话描述不能只是复述论文做了什么，而要说明为什么它比普通候选更值得读：核心贡献是什么，最强维度来自研究问题、方法新意、系统价值还是证据强度，以及有哪些可核验的方法、系统或证据信号。前端会在高分论文卡片和详情页展示这条高分信号。

`ICT` 只在论文主问题域确实属于通信/电信/网络基础设施时展示，例如 5G/6G、RAN/O-RAN、无线/蜂窝/移动/核心/边缘/光/卫星网络、网络切片、路由、QoS、频谱、切换、业务保障、告警关联或故障诊断。泛 AI agent、泛多智能体、泛 graph/neural network、社交网络或只出现 `network` 一词，不会自动打 `ICT` 标签，也不会获得网络自治/电信方向加分。

评分规则版本变更时，前端会自动清空浏览器中已有的推荐报告和打分结果，避免旧规则分数与新规则混用。查询条件、API Key 和模型设置不受影响。

## 推荐展示

推荐报告包含三种视图：

- 推荐论文：分数达到阈值的论文。
- 隐藏论文：分数低于阈值的论文，仍然可以打开查看，但不会计入最低达标数。
- 全部分析：本次已分析的所有论文。

单篇论文分析页用于连续阅读，会保留大模型生成的技术分析正文，不再展示摘要和维度条。

论文探索页会汇总历史推荐列表里的论文，方便跨列表浏览和搜索。

## 阅读清单

每个推荐列表都可以生成一份“精选论文阅读清单”。周报生成是独立功能，和原始推荐列表之间只传递论文子集：原列表的推荐/隐藏、旧分数、旧排序和旧评价不会被修改，也不会直接决定周报排序。

周报严格使用推荐列表生成日期所在的自然周，范围为本地时间周一 `00:00`（含）到下周一 `00:00`（不含）。推荐列表本身仍可使用“最近 7 天”“最近 14 天”等滚动窗口，但生成周报时，范围外论文会在前端候选阶段被排除，服务端收到请求后还会再次校验，因此上一自然周的论文不会进入本周复评、候选增补或 Markdown 正文。历史分析可以继续复用，但只有论文发布日期落在本周范围内才可能进入本周周报。浏览器中已经保存、但没有自然周范围记录的旧版周报会提示按自然周重新生成，不会被标记为已完成过滤。

生成阅读清单前，可以先调三个周报专用参数：

- 复评候选下限：只用于从原始列表所属自然周的论文中取一组子集，便于把摘要初筛时低于推荐线但仍有潜力的文章纳入复评。界面范围为 `0-95`，默认 `60`，按 `5` 分步进。
- 周报入选阈值：服务端抓取原文并重新给出四维分数后，只选择复评分达到该阈值的论文。界面范围为 `40-95`，默认沿用推荐阈值或 `70`，按 `5` 分步进。
- 达标篇数目标：界面范围为 `1-20`，默认 `3`。如果主候选中达到入选阈值的论文不足，服务端会继续从同周、未隐藏的增补池抓取原文并完成 Evidence、Review 与 Calibration。该数值是候选扩展目标，不会让低于入选阈值的论文自动入选。

前端会把同周可见论文拆成主候选和增补候选提交给服务端。服务端为候选抓取 arXiv HTML 原文并做本地缓存，然后基于原文摘录重新评价研究问题价值、方法新意、系统价值和证据强度，得到周报专用的新总分；同时根据作者区、机构脚注、邮箱域名、致谢或项目页提取发表单位线索。抓取失败或原文质量不合格的论文不会降级为摘要进入发布链路，而是被排除并继续尝试增补候选。周报复评产生的新分数、排序、发表单位线索和入选结果只用于本次周报，不回写原始推荐列表。

周报生成阶段包括：

```text
选择候选 -> 提交任务 -> 抓取原文 -> 周报复评 -> 生成 Markdown -> 接收结果 -> 保存到当前列表
```

当前的推荐列表、论文列表和已发布周报展示结果保存在浏览器的 `localStorage`。浏览器会把当前列表中的候选论文提交给服务端执行周报任务；服务端保存任务状态、Trace、阶段产物和原文缓存，但不保存推荐列表本身。因而更换浏览器、设备或清理站点数据后，历史列表不会自动恢复。后续将把推荐列表和发布周报元数据迁移到服务端持久化，浏览器只负责展示和操作；在完成迁移前，建议不要清理站点数据。

如果没有论文满足“复评候选下限”，前端会提示先调低候选下限。达到“周报入选阈值”的论文少于目标数量时，服务端会继续处理增补候选；增补池耗尽后，有 1–2 篇达标论文就按实际数量发布，没有达标论文才拒绝发布。低于阈值的论文不会用于补足篇数。

周报正文返回后必须通过服务端发布质量门才会保存到当前推荐列表。质量门会检查：

- YAML 的标题、描述、日期、月份、周次、分类、标签和论文数量。
- 正文一级标题、核心章节、逐篇必填小节、完整论文清单表头和固定尾注。
- 每篇入选论文是否在正文中出现，以及是否混入未入选的 arXiv 论文。
- 每篇论文是否只有一个对应的逐篇正文条目，条目中是否串入其他论文链接。
- 发布评分、高分维度和中文发表单位是否与周报复评结果一致。
- 百分比、时延、吞吐率等精确数字是否能在该论文的摘要、原文摘录或复评证据中找到。
- 是否有低于入选阈值的论文进入发布稿。
- 是否有未通过原文质量门、仅有摘要的论文进入发布稿。
- 每篇论文是否至少给出两条具体的局限与适用约束。
- 是否泄露复评阈值、候选增补、候选下限等内部流程词。

质量门不通过时接口会返回 `WEEKLY_REPORT_QUALITY_GATE_FAILED`，新结果不会覆盖浏览器中已经保存的上一份有效周报。

确定性质量门通过后，服务端默认再调用一次同一 LLM 做独立语义评审。语义评审不会改写周报，只返回结构化结论，检查逐篇事实依据、跨论文污染、趋势支撑、评分与语气、发表单位、摘要/原文证据边界和 ADN 启发是否具体。

语义评审支持三种模式：

| 模式 | 行为 |
| --- | --- |
| `off` | 不调用 LLM 语义评审，只保留确定性质量门。 |
| `warn` | 默认模式。`review`、`reject` 或评审不可用时仍保留生成结果，但标记“需人工复核”。 |
| `enforce` | 只有 `pass` 可以发布；`review`、`reject` 或评审不可用都会阻止发布。 |

LLM 语义评审是附加防线，不能替代确定性检查。即使模型自报 `pass`，只要返回 high 问题或关键检查项失败，服务端仍会把结果提升为 `reject`；返回字段残缺时不会静默当作通过。

阅读清单由 LLM 生成 Markdown，标题格式为：

```text
【精选论文】YY年M月第N周阅读清单：一句话观点
```

输出内容包括：

- YAML 前置元数据，包含 `title` 和 `description`，便于发布到洞察网站。
- 正文一级标题必须直接使用完整标题：`【精选论文】YY年M月第N周阅读清单：一句话观点`。冒号后的观点控制在 18-32 个中文字符以内，要提炼本周核心趋势或观点，避免复述标题或空泛表述。
- 报告导读，概括本周高价值论文、特别值得关注的文章，以及对华为 ADN 网络研究最有价值的方向。
- 本周趋势判断，提炼多篇论文共同呈现出的研究趋势、成熟度风险和对 ADN 的关联。
- 本周必读、值得跟进、快速扫读三个层级。
- 每篇论文的中文发表单位、阅读价值评分、符合的评分维度、内容方法与结果、ADN 启发与阅读价值、局限与适用约束。
- 推荐阅读顺序和完整论文清单；完整清单使用“论文、一句话介绍、阅读级别、链接”，不重复展示发表单位、阅读价值评分或符合维度，也不展示内部阈值、复评流程或方向列。
- 最后一行由服务端固定补齐发布署名、开源联系和工具/模型说明。

## API Key

API Key 可以在页面左上角齿轮设置里输入。Key 只保存在当前浏览器会话的 `sessionStorage`，不会写入项目文件。

也可以通过环境变量提供大模型接口配置：

```powershell
$env:LLM_API_KEY="your-api-key"
$env:LLM_MODEL="your-model"
$env:LLM_API_URL="https://your-llm-provider.example/api"
node server.js
```

常用环境变量：

- `LLM_API_KEY`
- `LLM_MODEL`
- `LLM_API_URL`
- `LLM_MAX_OUTPUT_TOKENS`：默认 `12000`，控制 LLM 单次分析的最大输出 token。
- `WEEKLY_REPORT_AGENT_MAX_OUTPUT_TOKENS`：默认 `65536`、最大 `128000`，仅控制新版周报 Agent 的结构化输出预算；不影响推荐列表分析。
- `LLM_RESPONSE_MAX_CHARS`：默认 `500000`，只做超大响应保护；不会把模型返回内容截断成短文本。
- 将周报输出预算提高到接近 `128000` 时，如确需接收极长响应，应同步评估 `LLM_RESPONSE_MAX_CHARS`；超过字符保护会明确失败，不会截断后继续发布。
- `LLM_REQUEST_TIMEOUT_MS`：默认 `600000`，控制 LLM 分析请求超时时间。
- `PAPER_ORIGINAL_TEXT_CACHE_DIR`：可选，覆盖论文原文缓存目录；本地测试使用临时目录避免污染项目缓存。
- `WEEKLY_REPORT_SEMANTIC_REVIEW_MODE`：`off`、`warn` 或 `enforce`，默认 `warn`。
- `WEEKLY_REPORT_SEMANTIC_REVIEW_MAX_TOKENS`：语义评审最大输出 token，默认 `3000`。
- `WEEKLY_REPORT_SEMANTIC_REVIEW_TIMEOUT_MS`：语义评审超时时间，默认 `180000`。
- `WEEKLY_REPORT_SEMANTIC_EVIDENCE_MAX_CHARS`：发送给语义评审的论文证据总预算，默认 `60000`。

## 配置

```powershell
$env:PORT=3000
$env:HOST="127.0.0.1"
```

`HOST` 或 `BIND_HOST` 用来控制监听地址。远端部署建议设置为 `127.0.0.1`，避免 Web 页面和 API 直接暴露在公网。

```powershell
$env:ARXIV_MIN_INTERVAL_MS=3500
$env:ARXIV_CACHE_TTL_MS=1800000
$env:ARXIV_STALE_CACHE_TTL_MS=86400000
$env:ARXIV_429_COOLDOWN_MS=1800000
$env:ARXIV_429_MAX_COOLDOWN_MS=7200000
$env:ARXIV_DAILY_SYNC_MS=72000000
$env:ARXIV_AUTO_SYNC=1
$env:ARXIV_AUTO_SYNC_INITIAL_DELAY_MS=30000
$env:ARXIV_AUTO_SYNC_RETRY_MS=3600000
$env:ARXIV_RSS_CATEGORIES="cs.NI,cs.AI,cs.LG,cs.MA,cs.DC,cs.IT,eess.SP,eess.SY"
```

## 本地运行

项目不依赖第三方 npm 包，使用 Node.js 原生 HTTP 服务。

```bash
node server.js
```

打开：

```text
http://localhost:3000
```

## 测试与检查

项目使用 Node.js 内置的 `node:test`，不增加第三方测试依赖。测试重点覆盖周报自然周边界、跨周过滤、论文去重、阈值选文与候选增补、发布 Markdown 质量门、逐篇语义一致性、精确数字证据和 API 契约。

其中完整主流程测试会在本机启动 Mock arXiv 和 Mock LLM，离线走完“原文抓取 → 逐篇复评 → 选文 → 周报生成 → 确定性质量门 → LLM 语义评审”，不会访问外网或产生模型费用。`test/fixtures/weekly-report/semantic-cases.json` 保存少量人工标注的语义风险样本；大改之后可配合 `WEEKLY_REPORT_READER_EXPERIENCE_CHECKLIST.md` 做一次五分钟人工体验验收。

```bash
npm test
```

运行前后端语法检查和全部测试：

```bash
npm run check
```

## 远端部署

远端建议使用 systemd 常驻运行，并让服务只监听远端本机：

```ini
Environment=NODE_ENV=production
Environment=PORT=3000
Environment=HOST=127.0.0.1
ExecStart=/usr/bin/node /home/<user>/paper-insight/server.js
```

远端不直接开放公网 `3000` 端口。需要访问远端页面时，在本机开启 SSH 隧道：

```powershell
& 'D:\Program Files\Git\usr\bin\ssh.exe' -i D:\code\paper-insight\.cache\paper_insight_ed25519 -N -L 3001:127.0.0.1:3000 <user>@<server>
```

然后打开：

```text
http://localhost:3001
```

### 本机 SSH 隧道守护

为避免 SSH 隧道在本机断开后需要手动重连，仓库提供了守护程序。守护程序每 10 秒请求本地周报任务接口；连接异常或它创建的 SSH 子进程退出时，会按 2、5、10、30 秒退避自动重连。它只终止自身创建的 SSH 子进程，不会操作远端服务、历史数据或其他手工 SSH 进程。

Windows 上，每台机器使用自己的服务器地址、SSH 用户和私钥安装登录后常驻任务。私钥和本机配置仅保存在 `.cache/tunnel-watchdog/`，该目录不进入 Git：

```powershell
.\scripts\install-tunnel-watchdog.ps1 -Install -RepositoryPath C:\work\code\paper-insight -Host <server> -User <user> -IdentityPath C:\keys\paper_insight_ed25519
```

安装后可查看状态或移除计划任务；卸载只删除计划任务和状态文件，不删除私钥、日志、远端数据或仓库文件：

```powershell
.\scripts\install-tunnel-watchdog.ps1 -Status -RepositoryPath C:\work\code\paper-insight
.\scripts\install-tunnel-watchdog.ps1 -Uninstall -RepositoryPath C:\work\code\paper-insight
```

首次排查可在前台运行。先通过安装脚本生成 `.cache/tunnel-watchdog/config.json`，再执行：

```powershell
npm run tunnel:watchdog -- --config .cache\tunnel-watchdog\config.json
```

日志位于 `.cache/tunnel-watchdog/tunnel-watchdog.log`，状态位于 `.cache/tunnel-watchdog/state.json`。守护核心是 Node 脚本，在其他操作系统可以使用同一份本机配置前台运行；Windows 的登录后自动启动由安装脚本负责。

常用远端命令：

```bash
sudo systemctl status paper-insight
sudo systemctl restart paper-insight
sudo journalctl -u paper-insight -f
```

## 项目结构

```text
server.js                 # Node HTTP 服务、arXiv 同步、候选筛选、LLM 分析和翻译接口
public/index.html        # 页面结构
public/styles.css        # 界面样式
public/app.js            # 前端交互、流程弹窗、报告展示、探索页和本地历史列表
weekly-report/rules.js   # 周报自然周过滤、去重、阈值和确定性选文规则
weekly-report/publish-guard.js # 周报发布前 Markdown 质量门
weekly-report/semantic-review.js # LLM 语义评审结果归一化和发布决策
test/                    # 固定样本、完整主流程、周报规则、发布质量和 API 回归测试
WEEKLY_REPORT_READER_EXPERIENCE_CHECKLIST.md # 重构后的语义与读者体验快速验收
PAPER_FETCHING_README.md # 论文获取业务逻辑说明
README.md                # 项目说明
```

## 待办

- 推荐列表持久化：将推荐列表和发布周报元数据迁移到服务端，消除浏览器本地存储对历史列表恢复的依赖。
- 周报任务运行态：在任务弹窗持续显示当前阶段、正在处理的论文、初稿/修正等调用类型、完成/排除/待处理数量；进度不得将“原文可用数”误作“已处理数”，并自动刷新 Trace 摘要。
- 管理员决策可读性：不得直接向管理员展示英文校验描述或内部规则码；每个待处理问题应说明涉及字段、触发的原句或内容、中文原因、必须满足的要求，以及“继续修正”将执行的具体操作。多项问题必须逐项说明；时间窗口、版本号等非实验数字不得笼统描述为“数字缺少证据”，应明确说明实际误判原因和可选处理结果。
- 原文抓取增强：在 arXiv HTML 之外继续支持 PDF 或 TeX 源文件抽取。
- 分析来源标记：在单篇推荐分析结果中标明本次分析是基于“摘要和元数据”还是“论文原文”。
- 给全文缓存增加大小上限和清理策略。

## 注意事项

- API Key 不应该提交到仓库。
- 本地缓存保存在 `.cache/`，该目录不会提交到 Git。
- 强制 arXiv API 可能遇到 `429 Rate exceeded`，这是 arXiv 服务端限流，不代表应用逻辑卡死。
- 远端部署时不要把服务直接绑定到 `0.0.0.0` 暴露公网，优先使用 SSH 隧道访问。
