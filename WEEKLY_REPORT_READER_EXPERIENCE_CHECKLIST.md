# 周报 Agent Loop 真实灰度检查单

真实运行中的问题、修复和回归用例统一记录在 `WEEKLY_REPORT_GRAY_ISSUE_REGISTRY.md`，不得只修提示词而不登记防护网。

适用于个人 Demo 的重构后真实灰度。最终标准是发布稿的可信度和阅读价值显著提升；Agent Loop 与 Trace 只用于管理员运维、解释和定位问题。

## 什么时候执行

- 修改周报选文、复评、生成或发布规则后。
- 修改 LLM 提示词、模型或评分逻辑后。
- 修改异步 Job、任务恢复、Trace 或发布保护后。

## 灰度前提

- 使用真实同周、可见推荐论文；隐藏论文不能放入 primary 或 reserve。
- 每篇论文必须能获取并通过 arXiv HTML 全文质量门，不允许摘要降级。
- 准备同一批论文的旧版周报 Markdown，作为盲评基线。
- 模型密钥只通过环境变量配置，不能写入灰度输入、输出或 Trace。

## 推荐执行方式

1. 复制 `test/fixtures/weekly-report/gray-input.example.json`，替换为真实候选。
2. 准备旧版 Markdown，例如 `old-weekly-report.md`。
3. 配置 `LLM_API_KEY` 或 GLM 对应环境变量。
4. 执行：

   `npm run gray:weekly-report -- --input real-gray.json --baseline old-weekly-report.md`

   所有真实运行问题、修复和回归用例同时登记到 `WEEKLY_REPORT_GRAY_ISSUE_REGISTRY.md`。

5. 打开生成目录中的 `result.md`、`trace.json` 和 `evaluation.md`，逐篇对照 arXiv HTML 原文完成签字。

## 语义正确性

- [ ] 周报中的论文都属于当前自然周，没有混入上一周内容。
- [ ] 每篇入选论文都有独立条目，没有遗漏或混入未入选论文。
- [ ] 论文 A 的方法、结果、机构、评分和链接没有写入论文 B。
- [ ] 精确数字、数据集、基线和实验结论可以在对应论文 Evidence 中找到。
- [ ] 所有关键事实均来自通过质量门的 arXiv HTML 全文，不依赖摘要降级。
- [ ] 趋势判断由多篇论文共同支撑；只有单篇支撑时使用克制表述。
- [ ] fallback 论文没有被包装成“本周必读”；background_only 分层保持不变。
- [ ] ADN 启发指向具体机制、假设、接口或风险，不是通用宣传口号。

## 读者体验

- [ ] 标题能说明本周具体技术信号，不是“新范式”“值得关注”等泛化表达。
- [ ] 导读能让读者快速知道收录了什么、最值得看什么。
- [ ] 不同阅读层级的差异清楚且理由可信。
- [ ] 每篇论文都能回答：解决什么问题、怎么做、证据怎样、限制是什么。
- [ ] 每篇都明确回答“为什么值得打开原文”，而不是复述摘要。
- [ ] 趋势章节没有重复逐篇摘要，而是给出跨论文判断。
- [ ] 正文没有出现 prompt、JSON、阈值、fallback、Trace 等内部信息。
- [ ] 管理员告警只出现在运维界面，读者正文不暴露内部流程。

## 失败保护

- [ ] reject、取消或调用失败后，上一份有效周报仍能查看和复制。
- [ ] 任一必需 QA 不可用或失败时终态为 reject，不产生第三种发布状态。
- [ ] 发布质量门失败时，不保存新稿，并保留被拒绝稿与完整 Trace。
- [ ] 浏览器关闭后任务继续；重新打开能恢复 running 或回挂已完成 publish。
- [ ] 管理员取消后终态为 reject，原因是 `admin_cancelled`。

## 显著提升判定

- [ ] 新稿严重事实问题为 0，且没有跨论文串写或无依据精确数字。
- [ ] 对旧稿、新稿隐藏来源盲评，新稿可信度评分明显更高。
- [ ] 新稿能更快回答“本周最值得读什么、为什么值得打开原文”。
- [ ] 新稿的趋势、分层和阅读顺序由实际论文证据支撑。
- [ ] 新稿增加的长度主要来自有效判断和证据边界，而不是重复摘要和空话。

## 验收记录

| 日期 | Trace ID | 使用模型 | 旧稿阅读价值 | 新稿阅读价值 | 严重事实问题 | 是否通过 |
| --- | --- | --- | ---: | ---: | ---: | --- |
|  |  |  |  / 5 |  / 5 |  |  |

通过标准：严重事实问题、跨论文串写和无依据精确数字均为 0；新稿可信度与阅读价值相对旧稿有明确、可解释的提升。自动 QA 通过只是进入人工灰度验收的前提，不等于最终通过。
